# =============================================================================
# Hi-Ring Environment Variables Configuration
# =============================================================================
#
# IMPORTANT: Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!
#
# =============================================================================

# Node Environment
NODE_ENV=development

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================

# MongoDB Connection String
# Format: mongodb://username:password@host:port/database
# Or Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb://localhost:27017/hiring-app
MONGODB_DB=hiring-app

# =============================================================================
# AUTHENTICATION (REQUIRED)
# =============================================================================

# NextAuth.js Base URL (your application URL)
NEXTAUTH_URL=http://localhost:3000

# NextAuth.js Secret (min 32 characters, generate with: openssl rand -base64 32)
# CRITICAL: Use a strong random string in production!
NEXTAUTH_SECRET=your-super-secret-key-here-minimum-32-characters

# CSRF Secret (optional - defaults to NEXTAUTH_SECRET if not set)
# Used for CSRF token generation and validation
# CSRF_SECRET=your-csrf-secret-key-here-minimum-32-characters

# -----------------------------------------------------------------------------
# Admin Email Whitelist (REQUIRED)
# -----------------------------------------------------------------------------
# Comma-separated list of emails allowed to access the admin area
# CRITICAL: Only these emails can authenticate to the application
# Format: email1@domain.com,email2@domain.com (no spaces)
ADMIN_EMAIL_WHITELIST=hugo@hi-ring.fr,izia@hi-ring.fr

# -----------------------------------------------------------------------------
# Admin User Passwords (OPTIONAL - for development initialization)
# -----------------------------------------------------------------------------
# These passwords are used by /api/dev/init-admins endpoint
# If not set, default secure passwords will be generated
# IMPORTANT: Change these immediately after first login!
HUGO_ADMIN_PASSWORD=HugoAdmin2025!@#
IZIA_ADMIN_PASSWORD=IziaAdmin2025!@#

# =============================================================================
# EMAIL CONFIGURATION (OPTIONAL)
# =============================================================================

# Email Provider: smtp, sendgrid, ses, mailgun
EMAIL_PROVIDER=smtp
EMAIL_FROM=noreply@your-domain.com
EMAIL_FROM_NAME=Hi-Ring Recrutement

# -----------------------------------------------------------------------------
# SMTP Configuration (if EMAIL_PROVIDER=smtp)
# -----------------------------------------------------------------------------
# For Gmail: Use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# -----------------------------------------------------------------------------
# SendGrid Configuration (if EMAIL_PROVIDER=sendgrid)
# -----------------------------------------------------------------------------
SENDGRID_API_KEY=your-sendgrid-api-key

# -----------------------------------------------------------------------------
# AWS SES Configuration (if EMAIL_PROVIDER=ses)
# -----------------------------------------------------------------------------
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# -----------------------------------------------------------------------------
# Mailgun Configuration (if EMAIL_PROVIDER=mailgun)
# -----------------------------------------------------------------------------
# MAILGUN_API_KEY=your-mailgun-api-key
# MAILGUN_DOMAIN=your-mailgun-domain

# =============================================================================
# STORAGE (OPTIONAL)
# =============================================================================

# Azure Storage Connection String (for file uploads)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string

# =============================================================================
# APPLICATION CONFIGURATION (OPTIONAL)
# =============================================================================

APP_NAME=Hi-Ring
APP_URL=http://localhost:3000

# =============================================================================
# RATE LIMITING (OPTIONAL)
# =============================================================================

# Note: Rate limiting now uses Redis when available, with automatic fallback
# to in-memory storage. Configure Redis for production deployments.

# Redis Configuration (for production rate limiting)
# Option 1: Use Redis URL (recommended for managed services like Upstash, Redis Cloud)
# REDIS_URL=redis://localhost:6379
# REDIS_URL=rediss://default:password@your-redis-host:6379

# Option 2: Use individual parameters
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password
# REDIS_DB=0

# Legacy environment variables (deprecated, but kept for backward compatibility)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=60
RATE_LIMIT_WINDOW_MS=60000

# =============================================================================
# SECURITY (OPTIONAL)
# =============================================================================

# CORS Configuration for Browser Extension and API Access
# IMPORTANT: In production, specify exact origins (comma-separated)
# In development, all origins are allowed for testing
# Format: origin1,origin2,origin3 (no spaces, no wildcards)
# Examples:
#   - chrome-extension://your-extension-id
#   - https://your-domain.com
#   - https://app.your-domain.com
ENABLE_CORS=false
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com,chrome-extension://your-extension-id

# =============================================================================
# MONITORING & ANALYTICS (OPTIONAL)
# =============================================================================

# Sentry DSN for error tracking
# SENTRY_DSN=your-sentry-dsn

# Vercel Analytics ID
# VERCEL_ANALYTICS_ID=your-analytics-id

# =============================================================================
# FEATURE FLAGS (OPTIONAL)
# =============================================================================

FEATURE_WORKFLOWS=true
FEATURE_EMAIL_TEMPLATES=true
FEATURE_CUSTOM_FIELDS=true

# =============================================================================
# NOTES
# =============================================================================
#
# Security Best Practices:
# 1. Never commit .env.local to git
# 2. Use strong random secrets (minimum 32 characters)
# 3. Rotate secrets regularly
# 4. Use different values for development and production
# 5. Limit access to production environment variables
#
# To generate a strong secret:
#   openssl rand -base64 32
#
# To generate a UUID:
#   openssl rand -hex 16
#
# =============================================================================
